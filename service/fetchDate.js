const getDate = () => {
    try {
        console.log("Datum")
        return WebTransportDatagramDuplexStream;
    } catch (error) {
        console.error('Login error: ' + error)
        return null;
    }
}

export {
    getDate
}