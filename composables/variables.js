const local_url = 'http://localhost:'
const local_port = 4000
const public_url = 'http://{PUBLIC_KEY}:'
const public_port = 4000

export {
    local_url,
    local_port,
    public_url,
    public_port,
}